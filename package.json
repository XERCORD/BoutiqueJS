{
  "name": "projet-boutique",
  "private": true,
  "description": "Boutique e-commerce — Express + frontend statique (déploiement Railway)",
  "scripts": {
    "postinstall": "npm ci --prefix backend --omit=dev",
    "start": "node backend/app.js"
  },
  "engines": {
    "node": ">=18"
  }
}
